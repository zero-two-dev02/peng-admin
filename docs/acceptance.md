# 验收记录

记录日期：2026-09-02。前端工作区 `D:\java-projects\yudao-admin`，分支 `master`，开发基线 `2b6c626`；后端契约基线为 `D:\java-projects\yudao-cloud` 的 `29eda07` 加当前未提交的 Product 修复。本文不记录密码、Token、Authorization、密钥或密码哈希。

## 结论分级

| 验收层级         | 结果     | 证据与边界                                                                                                                                                                                          |
| ---------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 类型检查         | 通过     | `pnpm typecheck`，`vue-tsc -b --pretty false` 退出码 0                                                                                                                                              |
| 单元/组件测试    | 通过     | `pnpm test`，5 个文件、53 项测试通过；覆盖请求拦截器、错误分类、安全返回路径、完整替换保护、金额/时间、请求竞态和状态组件                                                                           |
| 生产构建         | 通过     | `pnpm build`，Vite 8.2.0 转换 1715 个模块并生成 `dist`；领域页懒加载                                                                                                                                |
| 隔离浏览器测试   | 通过     | `pnpm test:e2e`，Edge/Playwright 17 项通过；测试内拦截 `/api`，不会被记为真实后端联调                                                                                                               |
| 响应式浏览器检查 | 通过     | 375、768、1024、1440；逐个访问 19 个业务导航入口，无页面级水平溢出                                                                                                                                  |
| 真实浏览器读取   | 通过     | 经 Vite `/api` 代理和 Gateway 登录；验证工作台、账户、System、Notice、Product、Member、Inventory、Payment 当前全部管理查询页，无页面脚本错误                                                        |
| 真实浏览器写入   | 部分通过 | System 关系替换/强退、Notice 完整生命周期、Product 类目/SPU/SKU 创建编辑及上下架、Member 最小积分调整、Inventory 初始化和调整通过；2026-09-02 主人提供的浏览器截图确认 Product 归档筛选与总数已恢复 |
| 真实鉴权         | 通过     | 匿名访问各管理 API 返回后端无效 Token 业务码；登录后菜单按权限快照展示，最终写鉴权仍由后端执行                                                                                                      |
| 后端打包         | 通过     | `mvn -s maven-settings.xml ... -DskipTests package`，BUILD SUCCESS，14.871 秒；该命令跳过测试，不等于后端测试通过                                                                                   |
| 差异检查         | 通过     | `git diff --check` 无空白错误；LF/CRLF 文本是 Windows 工作区转换提示                                                                                                                                |

最终一轮回归应在所有修正落盘后重新执行，以上数字以本任务最终报告为准。

## 真实浏览器覆盖

- 登录、当前用户、受保护路由、退出入口、刷新后重新登录、无权限与异常落点。
- 用户创建/停用、基本信息、角色完整替换、会话查询与强制下线；临时角色和临时权限完成创建、分配、解除和删除。
- 公告完成创建、编辑、发布、下线和删除，公告审计保留完整事实；审计筛选按数据库墙上时间提交和显示。
- 类目创建/编辑/启停，SPU 与初始 SKU 创建、编辑、上下架；2026-09-02 截图确认修复后归档筛选显示归档商品且总数为 1。
- 会员按 System 用户 ID 查询并执行一次最小正向积分调整；库存对新 SKU 初始化后执行一次最小正向调整，并查询对应不可变流水。
- 支付单、支付操作审计、回调事件、数量核对读取；支付单到回调事件筛选联动。浏览器没有调用内部接口或模拟支付回调。
- 真实服务不可用、HTTP 错误、业务错误和权限拒绝均有明确状态；危险写入二次确认，完整替换展示新增/移除差异。

## 验收数据与清理

- 临时公告、角色和权限已解除关联并删除。
- 验收用户保留为停用状态，因为 Member 档案及不可变积分流水已引用该 System 用户；未保留其密码。
- 验收类目、SKU 保留为停用/下架状态，验收 SPU 当前归档。后端没有类目和 SKU 删除接口。
- 一条会员积分流水和两条库存流水属于不可变业务审计事实，按验收约定保留；没有写入 Payment 回调、支付单、订单或库存预占。
- 隔离 E2E 的 `fixture_*`、`FIXTURE-*` 和测试 Token 只存在于 `tests/`，不会进入生产页面或真实后端。
- `dist/`、`test-results/`、`.local-run/` 已忽略；未执行 Git 暂存、提交、推送或部署。

## 未声称通过

- 个人密码修改没有在真实管理员账号执行，避免改变主人的账号凭据；该交互仅有自动化覆盖。
- 后台订单管理、会员目录/等级/权益、售后退款、经营统计和完整收银台没有后端契约，入口保持隐藏。
- Product 恢复动作没有由本轮浏览器截图独立验证；截图只证明归档状态及分页总数已显示正确。

Mock 只用于隔离自动化测试，不能替代以上真实验收边界。
